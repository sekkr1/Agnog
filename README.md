# Agnog - Agron database parser

A tool to export the Agron database file `3.CVF` to a CSV file.

## How to use

```bash
$ go get golang.org/x/text/encoding/charmap
$ go build
$ ./agnog -h
Usage: agnog cvf_file [output]
  -v    Verbose
```